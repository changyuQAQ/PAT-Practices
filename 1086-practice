int main()
{
	int n1, n2; cin >> n1 >> n2;
	string num = to_string(n1 * n2);
	while (num.back() == '0') num.pop_back();
	for (int i = num.size()-1; i  >=0; i--)cout << num[i];
}
//相乘后转换为字符串，并删掉尾部0，倒序输出
