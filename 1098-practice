int main()
{
	int n; cin >> n; vector<int>v1(n), v2(n);
	int num;
	for (int i = 0; i < n; i++) cin >> v1[i];
	for (int i = 0; i < n; i++) cin >> v2[i];
	sort(v1.begin(), v1.end());
	sort(v2.begin(), v2.end());
	int h = v1[0] - v2[n - 1];
	if (h > 0) {
		cout << "Yes " << h;
	}
	else {
		cout << "No " << (-h + 1);
	}
}
//通过隧道问题，else输出需要削掉的高度，误理解成需要挖掘的高度，浪费了很多时间
