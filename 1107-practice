int main()
{
	int n, m; cin >> n >> m; int ma1=-1;
	for (int i = 0; i < n; i++)
	{
		int ma = -1;
		for (int j = 0; j < m; j++)
		{
			int num; cin >> num;
			ma = max(num, ma);
		}
		cout << ma;
		if (i != n - 1)cout << " ";
		ma1 = max(ma, ma1);
	}
	cout << endl << ma1;
}
//没撒讲的
