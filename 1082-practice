int main()
{
	int n1; cin >> n1; int a1 = -1,a2,b2, b1 = 10005;
	for (int i = 0; i < n1; i++)
	{
		int a, b, c;
		cin >> a >> b >> c; b=abs(b); c=abs(c);
		int num = sqrt(b * b+c*c); if (b == 0)num = c; if (c == 0)num = b;
		if (num > a1)a2 = a, a1 = num; if (num < b1)b2 = a, b1 = num;
	}
	printf("%04d %04d", b2, a2);
}
//使用绝对值计算，坐标为零直接赋值
